### This file was generated by Nexus Schema
### Do not make changes to this file directly


input AddPostInput {
  authorId: Int!
  content: String
  published: Boolean!
  title: String!
}

input AddUserInput {
  email: String!
  name: String!
}

type Mutation {
  addPost(addPostInput: AddPostInput!): Post
  addUser(addUserInput: AddUserInput!): User
  deletePost(postId: ID!): User
  editPost(addPostInput: AddPostInput, postId: ID!): Post
}

type Post {
  authorId: Int!
  content: String
  createdAt: String!
  id: ID!
  published: Boolean!
  title: String!
  updatedAt: String!
}

type Profile {
  bio: String
  id: ID
  userId: Int
}

type Query {
  post(id: ID!): Post
  posts: [Post!]!
  user(id: ID!): User
  users: [User!]!
}

type User {
  email: String!
  id: ID!
  name: String!
  posts: [Post]
  profile: Profile
}
